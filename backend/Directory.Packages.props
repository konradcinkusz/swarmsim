<Project>
  <PropertyGroup>
    <CentralPackageVersionOverrideEnabled>false</CentralPackageVersionOverrideEnabled>
  </PropertyGroup>
  <ItemGroup>
    <!-- Test-only dependencies, plus one recorded runtime exception. Runtime code
         (SwarmApi.Domain/Application/Infrastructure/ServiceDefaults/Api) otherwise ships
         zero third-party NuGet packages by design — everything else it needs (minimal
         APIs, System.Net.WebSockets, health checks) ships in the ASP.NET Core shared
         framework. Per REPO-BASELINE.md §4b's dependency budget: this table is the whole
         budget. JWT bearer validation is the one addition that does not ship in-box —
         see docs/adr/0005-mcp-server-and-bearer-auth.md for why it was added rather than
         hand-rolled. -->
    <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="18.10.1" />
    <PackageVersion Include="xunit" Version="2.9.3" />
    <PackageVersion Include="xunit.runner.visualstudio" Version="4.0.0" />
    <PackageVersion Include="Microsoft.AspNetCore.Mvc.Testing" Version="8.0.31" />
    <PackageVersion Include="Microsoft.AspNetCore.TestHost" Version="8.0.31" />
    <PackageVersion Include="coverlet.collector" Version="10.0.1" />
    <PackageVersion Include="Microsoft.AspNetCore.Authentication.JwtBearer" Version="8.0.22" />
  </ItemGroup>
</Project>
